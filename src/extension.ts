/**
 * MemPalace Pi Extension
 *
 * This module is the entry point loaded by Pi when the extension is enabled.
 * It bootstraps tools, commands, and hooks registration.
 */

// Re-export the default extension factory
export { default } from "./extension/index";
